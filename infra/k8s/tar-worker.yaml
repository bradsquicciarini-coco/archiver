apiVersion: v1
kind: ServiceAccount
metadata:
  name: data-archiver-tar-builder
  namespace: foxglove
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::270582689972:role/data-archiver-role
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-archiver-tar-builder
  namespace: foxglove
spec:
  replicas: 4
  selector:
    matchLabels:
      app: data-archiver-tar-builder
  template:
    metadata:
      labels:
        app: data-archiver-tar-builder
        tags.datadoghq.com/service: data-archiver-tar-builder
        tags.datadoghq.com/env: dev
    spec:
      serviceAccountName: data-archiver-tar-builder
      nodeSelector:
        disk: "instance-store"
        karpenter.sh/capacity-type: spot
      containers:
        - name: data-archiver
          image: 270582689972.dkr.ecr.us-west-2.amazonaws.com/data-archiver-tar-builder:9352d47
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          env:
            - name: AWS_REGION
              value: us-west-2
            - name: QUEUE_URL
              value: https://sqs.us-west-2.amazonaws.com/270582689972/data-archiver-tar-worker
            - name: LOG_LEVEL
              value: debug 
            # - name: DISABLE_DESTRUCTIVE_ACTIONS
            #   value: "1"
          resources:
            requests:
              cpu: "1000m"
              memory: "1024Mi"
              ephemeral-storage: "128Gi"
            limits:
              cpu: "4000m"
              memory: "7096Mi"
              ephemeral-storage: "256Gi"
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 256Gi
